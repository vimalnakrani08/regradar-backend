# OpenAI API key for embeddings — get one at https://platform.openai.com
OPENAI_API_KEY=sk-...

# Database connection (defaults work for local Docker setup)
DATABASE_URL=postgresql+asyncpg://regradar:localdev@localhost:5432/regradar